//Contains the reference to the scripture and makes it look nice
public class Reference{
    string mainReference = "";
    List<string> verseList = new List<string>();
    public void input(string verse){
        mainReference = verse;
        int dashIndex = verse.IndexOf("-");
        int colonIndex = verse.IndexOf(":");
        if(dash != -1){
            while
        }
    }

    public string getReference(){
        return mainReference;
    }
}